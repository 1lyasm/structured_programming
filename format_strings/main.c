#include <stdio.h>

int main(void) {
    int number = 65;

    printf("Number as long int: %li\n", number);
    printf("Number as unsigned int: %u\n", number);
    printf("Number as character: %c\n", number);

    return 0;
}
